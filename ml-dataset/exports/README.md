# Model Exports

Place Create ML exports here:

```text
CaptionSceneClassifier_v1.mlmodel
CaptionSceneClassifier_v2.mlmodel
```

Model binaries are ignored by Git by default. After evaluation, drag the chosen `.mlmodel` into `SnapCopy/MLModels/` in Xcode and ensure the app target is checked.

Current local v1 model candidate has been copied to:

```text
ml-dataset/exports/CaptionSceneClassifier_v1.mlmodel
```

Original source:

```text
模型训练相关/models/SnapCopySceneClassifier.mlmodel
```

The app currently loads `SnapCopySceneClassifier` by default.
